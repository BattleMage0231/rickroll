use rickroll::lexer::*;
use rickroll::util::*;

fn main() {
    /*
    println!("{}", SPECIAL.contains('='));
    let s = String::from("TRUE || FALSE > 4 + 5.00 UNDEFINED ARRAY ARRAY ARRAY");
    println!("{}", s);
    let l = Lexer::new(s.clone());
    let x = l.make_tokens();
    match x {
        Ok(a) => println!("{:?}", a),
        Err(err) => println!("{}", err),
    }*/
    /*
    let x: Token = Token::Value(RickrollObject::Array(vec![RickrollObject::Int(4)]));
    println!("{:?}", x);
    */
}
